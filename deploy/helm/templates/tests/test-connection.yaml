apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pgdw-app.fullname" . }}-test-connection"
  labels:
    {{- include "pgdw-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pgdw-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
